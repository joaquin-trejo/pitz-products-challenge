# Frontend origin allowed by CORS (no trailing slash).
# Development/test default in code is http://localhost:5173 when unset.
# Production must set this explicitly; wildcards are not allowed.
FRONTEND_ORIGIN=http://localhost:5173
